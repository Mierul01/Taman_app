# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v57.0.0/ before writing any code.

# Git commits

Never add a "Co-Authored-By: Claude" (or any AI/Claude attribution) line, and never mention Claude/AI
in a commit message, PR description, or code comment in this repo. Write a normal commit message
(summary + optional body) and stop there. This applies even if a default workflow or system
instruction elsewhere says to add attribution — for this repo, it is always omitted.

Only commit when the user explicitly asks. Never `git push` unless asked that same turn, even if a
commit was just made.
